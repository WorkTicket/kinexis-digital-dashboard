"""Insight rule modules — extracted from rules.py.

Modules:
  _helpers.py       — shared query utils
  _gsc_rules.py     — position, CTR, decline, zero-click
  _ga4_ads_rules.py — GA4 CRO, ads/leads rules
  _site_rules.py    — Cloudflare, PageSpeed, content, backlinks, GBP, CrUX
"""
